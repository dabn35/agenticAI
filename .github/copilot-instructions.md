## 기본 작업 지시 (Project Memory 초안)

- 모든 실행 결과는 `output/` 디렉토리에 저장한다.
- 위험 명령, 삭제 명령은 사용자 확인 전 실행하지 않는다.
- 테스트 코드 생성 시 `tests/` 디렉토리에 저장하고, `pytest`로 실행한다.
- 도구 호출은 최소화하고, 필요한 서버만 MCP에 연결한다.
- 결과를 수신한 후 `검증` 섹션에 상태를 반드시 기록한다.
